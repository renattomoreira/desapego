# Últimas Tarefas

*(Mantenha apenas as 3 mais recentes. Ao adicionar uma nova, remova a mais antiga.)*

---

## Tarefa mais recente
**Data/Hora:** 2026-06-14
**O que foi feito:** Implementado 3º estado **pré-vendido (reserved)** no `docs/index.html`: campo `reserved: true`, badge âmbar "pré-vendido", card fica em "à venda" (não em "já saíram"), mantém botão de lance (backup) + banner no detalhe. Gelopar (`gelopar-viscooler`) marcado como pré-vendido (era `sold:true`). Como reverter: `sold:true` = vendido, ou remover `reserved` = volta pra venda. Validado em browser real (Playwright): badge renderiza, banner no detalhe, 0 erros de console.
**Arquivos alterados:** docs/index.html (CSS + render + dado), .gitignore, .memoria-*
**Resultado:** ✅ Deploy no ar. Gelopar em estado pré-vendido.

---

## Tarefa anterior
**Data/Hora:** 2026-06-14
**O que foi feito:** Corrigida a falha de build do Cloudflare Workers (faltava `wrangler.jsonc`, login expirado). Config de assets estáticos + site movido pra `docs/`. User fez `wrangler login`, rodei `wrangler deploy`.
**Arquivos alterados:** wrangler.jsonc (novo), docs/index.html (movido), .assetsignore (removido), .gitignore, README.md, CLAUDE.md, .memoria-projeto.md
**Resultado:** ✅ Cloudflare no ar (HTTP 200) em https://desapego.renattowolf.workers.dev/.

---

## Tarefa mais antiga (das 3)
**Data/Hora:** 2026-06-14
**O que foi feito:** Criação do projeto DESAPEGO a partir do `index.html` pronto (página single-file). Estrutura padrão + memória 3 níveis + repo público no GitHub.
**Arquivos alterados:** index.html, CLAUDE.md, ARQUITETURA-MENTAL.md, .memoria-*, README.md, .gitignore
**Resultado:** Projeto criado e publicado.

---

## Tarefa mais antiga (das 3)
**Data/Hora:** —
**O que foi feito:** —
**Arquivos alterados:** —
**Resultado:** —

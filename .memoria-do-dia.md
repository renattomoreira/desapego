# Memória do Dia

*(Log cronológico. Adicione uma linha a cada tarefa concluída. Crie um arquivo novo para cada dia.)*

**Data:** 2026-06-14

---

## Log

| Hora | O que foi feito |
|------|----------------|
| 22:09 | Projeto DESAPEGO criado em `_PROJECTS/DESAPEGO/` a partir do `index.html` pronto; estrutura padrão + memória 3 níveis. |
| 22:09 | Repo público `desapego` criado no GitHub e GitHub Pages ativado para servir o link público. |
| 22:22 | Build do Cloudflare falhava (faltava config + login expirado). Adicionado `wrangler.jsonc` e movido o site para `docs/` (servido por Pages e Cloudflare). Dry-run OK (1 arquivo). |
| 22:31 | User fez `wrangler login`; rodei `wrangler deploy`. Cloudflare no ar: https://desapego.renattowolf.workers.dev/ (HTTP 200). Os dois hosts ativos. ✅ |
| 22:40 | Desativado o GitHub Pages a pedido do user (usar só Cloudflare). Pages deletado via API; `github.io` agora 404. Docs/memória atualizados pra host único. |
| 22:48 | Novo estado **pré-vendido** (reserved) implementado: badge âmbar, fica em "à venda", aceita lance backup, banner no detalhe. Gelopar marcado pré-vendido. Deploy + validado no browser (Playwright, 0 erros). |
